

  // Vérifier si la balise iframe est présente
    if (window.self === window.top) {
      // Rediriger l'utilisateur
      window.location.href = 'https://pikayutmg.github.io/redirect/beta';
    }
